import { TaskItemRoot } from "./TaskItemRoot";
import { TaskItemLabel } from "./TaskItemLabel";
import { TaskItemTrash } from "./TaskItemTrash";

export const TaskItem = {
    Root: TaskItemRoot,
    Label: TaskItemLabel,
    Trash: TaskItemTrash,
}